# 🔧 COMMANDES GIT POUR VOTRE PROJET

## 1. Initialisation du dépôt

cd C:\chemin\vers\votre\projet
git init
git config user.name "Idris BR"
git config user.email "idrisbr52@gmail.com"

## 2. Commits progressifs (IMPORTANT : ne pas tout commit en une fois !)

# Premier commit - Structure de base
git add Cargo.toml Authors.md README.md .gitignore
git commit -m "feat: structure initiale du projet slab allocator"

# Deuxième commit - Module slab
git add src/lib.rs src/slab.rs
git commit -m "feat: implémentation du module slab avec allocation/libération"

# Troisième commit - Module cache
git add src/cache.rs
git commit -m "feat: ajout du cache de slabs et allocateur global"

# Quatrième commit - Tests
git add src/tests.rs
git commit -m "test: tests unitaires pour slab et cache"

# Commits supplémentaires pendant le développement
git add .
git commit -m "fix: correction gestion liste libre dans slab"

git add .
git commit -m "refactor: optimisation recherche de slab dans cache"

git add .
git commit -m "docs: ajout documentation safety pour zones unsafe"

git add .
git commit -m "test: ajout tests pour slabs pleins et vides"

## 3. Création du bundle pour soumission

git bundle create slab-allocator.bundle --all

## 4. Vérification du bundle

git bundle verify slab-allocator.bundle
git bundle list-heads slab-allocator.bundle

## 5. Pour tester votre bundle (simulation de récupération)

# Dans un autre dossier
git clone slab-allocator.bundle test-bundle
cd test-bundle
git log --oneline

## 6. Voir l'historique des commits

git log --oneline --graph --decorate
git log --stat

## 7. Commandes utiles pendant le développement

# Voir le statut
git status

# Voir les différences
git diff

# Voir l'historique complet
git log -p

# Voir qui a modifié quoi
git blame src/slab.rs

# Annuler des modifications non commitées
git checkout -- fichier.rs

## ⚠️ RAPPELS IMPORTANTS

1. NE PAS commit tout en une fois → considéré comme triche
2. Travailler régulièrement et commit progressivement
3. Maximum 2 personnes par groupe → mettre les 2 emails dans Authors.md
4. Le fichier .git DOIT être inclus via git bundle
5. Documentation Safety OBLIGATOIRE pour chaque bloc unsafe
6. Tests OBLIGATOIRES (miri/fuzzers = bonus)

## 📦 Soumettre votre projet

Envoyez le fichier: slab-allocator.bundle
